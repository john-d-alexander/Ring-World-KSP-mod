NIVEN_RINGWORLD
{
    // Metres, seconds, m/s^2. 1:10 linear scale of the published ring dimensions.
    radius = 15300000000
    width = 160500000
    wallHeight = 160000
    gravity = 9.72
    // Gameplay adaptation: a three-hour illumination cycle. Spin period is derived from g/R.
    daySeconds = 10800
    // Blank: choose a random seed once per new save. Existing saves keep their stored seed.
    seed =
    atmosphere = true
    atmosphereHeight = 60000
    scaleHeight = 8000
    // Minimum structure below deepest ocean floor (-1200 m), metres; not a claim about canonical scrith thickness.
    structuralThickness = 100
    tileSize = 1024
    tileResolution = 32
    tileRadius = 3
}
